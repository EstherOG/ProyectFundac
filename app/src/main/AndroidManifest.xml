<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.esther.menu">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Login"></activity>
        <activity android:name=".Gracias" />
        <activity android:name=".Pago" />
        <activity android:name=".Contabilidad" />
        <activity android:name=".Termino" />
        <activity android:name=".coment" />
        <activity android:name=".Contacto" />
        <activity android:name=".TienesFunda" />
        <activity
            android:name=".ui.login.Login"
            android:label="@string/title_activity_login" />
        <activity android:name=".perfil" />
        <activity android:name=".funda" />
        <activity android:name=".local" />
        <activity android:name=".Filtro" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>